# Core ML (installed on Colab for training; heavy, skip for local Flask-only dev)
torch>=2.2.0
transformers>=4.41.0
peft>=0.11.0
accelerate>=0.30.0
scikit-learn>=1.4.0
pandas>=2.2.0
numpy>=1.26.0
shap>=0.45.0
sentence-transformers>=3.0.0
fasttext-wheel>=0.9.2

# Web app
Flask>=3.0.0
Flask-SQLAlchemy>=3.1.0
alembic>=1.13.0
python-dotenv>=1.0.0
psycopg[binary]>=3.2.0

# LLM APIs
anthropic>=0.30.0
openai>=1.30.0    # OpenRouter uses the OpenAI-compatible client

# Reporting (from prior project experience)
reportlab>=4.2.0
